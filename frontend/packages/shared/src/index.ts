/**
 * Point d'entrée du paquet @orbita-ai/shared (tech/PROJECT_STRUCTURE.md §2).
 * Regroupera les utilitaires partagés entre applications frontend (formatage,
 * constantes de configuration, helpers de types).
 *
 * Squelette architectural uniquement : aucune fonctionnalité, aucun utilitaire
 * métier n'est implémenté à ce stade (cf. planning/DEVELOPMENT_PHASES.md).
 */
export const ORBITA_AI_SHARED_PACKAGE_MARKER = "orbita-ai-shared" as const;
