namespace OrbitaAI.Modules.UpdateManager.Application;

/// <summary>
/// Point d'entrée du module UpdateManager exposé aux autres modules et à la couche Présentation
/// (architecture/APPLICATION_LAYERS.md). Responsabilité unique du module (architecture/MODULE_ARCHITECTURE.md) :
/// gérer la distribution, l'application et la traçabilité des mises à jour du logiciel (tech/UPDATE_SYSTEM.md).
///
/// Squelette structurel uniquement : aucune méthode métier n'est déclarée à ce stade,
/// conformément au périmètre de cette mission (aucune logique, aucun calcul, aucun traitement).
/// </summary>
public interface IUpdateManagerService
{
}
