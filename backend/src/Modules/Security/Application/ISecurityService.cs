namespace OrbitaAI.Modules.Security.Application;

/// <summary>
/// Point d'entrée du module Security exposé aux autres modules et à la couche Présentation
/// (architecture/APPLICATION_LAYERS.md). Responsabilité unique du module (architecture/MODULE_ARCHITECTURE.md) :
/// garantir l'authentification, les droits d'accès et la protection des données, de façon transversale (tech/SECURITY_REQUIREMENTS.md).
///
/// Squelette structurel uniquement : aucune méthode métier n'est déclarée à ce stade,
/// conformément au périmètre de cette mission (aucune logique, aucun calcul, aucun traitement).
/// </summary>
public interface ISecurityService
{
}
