namespace OrbitaAI.Modules.ExportEngine.Application;

/// <summary>
/// Point d'entrée du module ExportEngine exposé aux autres modules et à la couche Présentation
/// (architecture/APPLICATION_LAYERS.md). Responsabilité unique du module (architecture/MODULE_ARCHITECTURE.md) :
/// extraire des données ou rapports de la plateforme vers des formats exploitables en dehors du système (features/EXPORT_ENGINE.md).
///
/// Squelette structurel uniquement : aucune méthode métier n'est déclarée à ce stade,
/// conformément au périmètre de cette mission (aucune logique, aucun calcul, aucun traitement).
/// </summary>
public interface IExportEngineService
{
}
