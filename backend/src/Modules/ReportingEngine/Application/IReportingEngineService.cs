namespace OrbitaAI.Modules.ReportingEngine.Application;

/// <summary>
/// Point d'entrée du module ReportingEngine exposé aux autres modules et à la couche Présentation
/// (architecture/APPLICATION_LAYERS.md). Responsabilité unique du module (architecture/MODULE_ARCHITECTURE.md) :
/// assembler les données et analyses en rapports formalisés, destinés au partage ou à l'archivage (features/REPORTING_ENGINE.md).
///
/// Squelette structurel uniquement : aucune méthode métier n'est déclarée à ce stade,
/// conformément au périmètre de cette mission (aucune logique, aucun calcul, aucun traitement).
/// </summary>
public interface IReportingEngineService
{
}
