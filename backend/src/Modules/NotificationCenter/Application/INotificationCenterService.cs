namespace OrbitaAI.Modules.NotificationCenter.Application;

/// <summary>
/// Point d'entrée du module NotificationCenter exposé aux autres modules et à la couche Présentation
/// (architecture/APPLICATION_LAYERS.md). Responsabilité unique du module (architecture/MODULE_ARCHITECTURE.md) :
/// détecter et acheminer les alertes et notifications vers les utilisateurs concernés (features/NOTIFICATION_CENTER.md).
///
/// Squelette structurel uniquement : aucune méthode métier n'est déclarée à ce stade,
/// conformément au périmètre de cette mission (aucune logique, aucun calcul, aucun traitement).
/// </summary>
public interface INotificationCenterService
{
}
